/**
 * Ambient declarations for the Vite build.
 *
 * `vite/client` supplies the module declarations for side-effect imports such as
 * `import './styles/tokens.css'`, and for `import.meta.env`. Without it, both
 * are type errors under `strict`.
 */
/// <reference types="vite/client" />
